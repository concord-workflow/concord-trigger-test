flows:
  default:
    - log: "Hello!"
  onPush:
    - log: "Commit ID: ${event.payload.after}"

triggers:
  - github:
      version: 2
      entryPoint: onPush
      useEventCommitId: true
      conditions:
        type: "push"
        branch: ".*"

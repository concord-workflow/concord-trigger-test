flows:
  default:
    - log: "Hello!"
  onPush:
    - log: "Got: ${event}"

triggers:
  - github:
      version: 2
      entryPoint: onPush
      conditions:
        type: "push"
        branch: ".*"

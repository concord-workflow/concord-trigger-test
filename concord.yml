flows:
  default:
    - log: "Hello!"
  onPush:
    - log: "Got ${event}"

triggers:
  - github:
      version: 1
      entryPoint: onPush
      useEventCommitId: true
      type: "push"
      branch: ".*"

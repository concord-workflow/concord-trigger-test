flows:
  default:
    - log: "Hello!"
  onPush:
    - log: "We got ${event}"

triggers:
  - github:
      version: 2
      entryPoint: onPush
      conditions:
        type: "push"
        branch: ".*"
